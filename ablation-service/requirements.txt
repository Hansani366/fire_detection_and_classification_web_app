# NO MODEL LIBRARIES HERE. The trained models moved to
# fire-classification-service, which owns them and pins the exact
# scikit-learn / xgboost / pandas that wrote the pickles. This service only
# applies rules and computes metrics, so it needs neither sklearn nor xgboost
# nor joblib -- and keeping them would invite someone to load a second copy of
# the weights here.
#
# pandas stays pinned to the model's version anyway: the rolling-window
# semantics used by the rules and the warm-up profile should match the ones the
# model was fitted with, so the two sides describe the same 25-second window.
pandas==3.0.6
numpy>=2

fastapi
uvicorn[standard]
python-multipart
httpx

# Reads recorded frames so flame flicker can be measured from them. Only the
# recordings path needs it.
pillow
